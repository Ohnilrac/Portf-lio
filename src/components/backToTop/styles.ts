import styled from "styled-components";

export const Button = styled.a`
  font-size: 3.5rem;
  
  position: fixed;
  right: 1rem;
  bottom: 4rem;

  /* visibility: hidden; */
  /* opacity: 0; */

  transform: translateY(100%);
  transition: 0.8s;
`